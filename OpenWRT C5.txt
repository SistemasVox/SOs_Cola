OpenWRT C5 "C:\Program Files\PuTTY\putty.exe" root@192.168.2.1 22 -pw xxxx

opkg update
opkg install screen
opkg install nano

nano /etc/rc.local

sleep 60

if command -v screen >/dev/null 2>&1; then
    # If screen is installed, use it to run the commands
    screen -dmS ping1 ping -s 756 1.1.1.1 &
    screen -dmS ping2 ping -s 756 9.9.9.9 &
else
    # If screen is not installed, run the commands directly in the background
    ping -s 756 1.1.1.1 &
    ping -s 756 9.9.9.9 &
fi

exit 0

chmod +x /etc/rc.local

screen -ls
ps | grep ping
